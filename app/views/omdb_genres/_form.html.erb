<%= form_for(@omdb_genre) do |f| %>
  <% if @omdb_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@omdb_genre.errors.count, "error") %> prohibited this omdb_genre from being saved:</h2>

      <ul>
      <% @omdb_genre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :genre_id %><br>
    <%= f.number_field :genre_id %>
  </div>
  <div class="field">
    <%= f.label :omdb_movie_id %><br>
    <%= f.number_field :omdb_movie_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
